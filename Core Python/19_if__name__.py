'''when you import py file to anothor file so the content of imported file also run with existing file if you 
don't want so you can use'''
#(if __name__ = '__main__')

import sum
print(sum.add(4,4))
